# ── LOCAL DEV ──────────────────────────────────────────────────────────────────
# Frontend calls go through Next.js proxy → backend (no CORS)
NEXT_PUBLIC_API_URL=/api/proxy

# Backend address used by the server-side proxy route
BACKEND_URL=http://localhost:5000

# Optional
NEXT_PUBLIC_RAZORPAY_KEY=rzp_test_xxxxxxxxxxxx
NEXT_PUBLIC_WHATSAPP_NUMBER=919876543210
NEXT_PUBLIC_UPI_ID=lonazluxe@upi
NEXT_PUBLIC_SALON_NAME=Lonaz Luxe Salon

# ── PRODUCTION (Vercel) ────────────────────────────────────────────────────────
# Set these in Vercel dashboard → Settings → Environment Variables:
# NEXT_PUBLIC_API_URL = /api/proxy
# BACKEND_URL         = https://your-api.onrender.com
